const drawer = {
  baseStyle: {
    overlay: {
      bg: "rgba(0, 0, 0, 0.7)",
      backdropFilter: "blur(6px)"
    },
    dialog: {
      bg: "surfaceAccent",
      borderLeft: "1px solid",
      borderColor: "border",
      boxShadow: "card",
      borderRadius: "2xl"
    }
  }
}

export default drawer
